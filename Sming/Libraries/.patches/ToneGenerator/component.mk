# This is a temporary patch until I2S support is added to the framework for other SoCs
COMPONENT_SOC := esp8266 host

COMPONENT_INCDIRS := src/include
COMPONENT_SRCDIRS := src
COMPONENT_DOXYGEN_INPUT := src/include
